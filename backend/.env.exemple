# Server
SERVER_PORT=3000
# Database
DB_HOST=postgres
DB_PORT=5432
DB_USER=<defune your user>
DB_PASSWORD=<define your password>
DB_NAME=<define db name>

# Mongo (jeśli używasz)
MONGO_URI=mongodb://admin:root@mongo:27017

# JWT secret (use strong random string in production)
JWT_SECRET=your_jwt_secret_here

